[project]
name = "gofish"
version = "0.1.0"
description = "Multiplayer Go Fish with AAC support."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.8",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
